export const metadata = { title: "Returns | Aurent", description: "Returns and refunds policy for Aurent orders." };

export default function ReturnsPage() {
  return (
    <section className="py-12">
      <div className="mx-auto max-w-3xl px-6 lg:px-8">
        <h1 className="mb-6 font-display text-4xl font-light">Returns</h1>
        <p className="text-stone">We accept returns within 14 days of delivery if the watch remains unworn, with all original packaging and documentation intact. Returns are inspected and processed within 5 business days. Custom orders and modified watches are not eligible for return.</p>
      </div>
    </section>
  );
}
